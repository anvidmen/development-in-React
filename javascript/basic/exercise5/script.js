const PASS_EXAM = 5
const testScore = 4.6

if (testScore < PASS_EXAM) alert(`Ooh, he failed the exam with a ${testScore}`)
// if (testScore < PASS_EXAM) alert('Ooh, he failed the exam with a ' + testScore)
