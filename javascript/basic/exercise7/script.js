const objects = ['table', 'chair', 'computer', 'notebook']

for (let i = 0; i < objects.length; i++) {
  console.log(`The ${objects[i]} object is at position ${[i]}`)
  // console.log('The ' + objects[i] + ' object is at position ' + [i])
}
